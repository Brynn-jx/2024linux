父子进程之间共享文件描述符表，这种共享是父子进程分别的文件描述符表指向了内核空间中的文件表的相同的表项，所以实现了共享的操作，可以使用close避免共享
send会阻塞，当send指定的发送大小缓冲区不满足此大小时，就会发生阻塞，当客户端断开时，send可以发送缓冲区剩余大小的文件大小，再发送send会返回-1，再发送send会发送SIGPIPE信号，终止进程
同级参数传递都是生成的副本，例如函数的参数时一个指针类型，调用方也传输的是一个指针类型，那么就会生成副本，所有就会导致修改函数内指针的内容对调用方没影响，但是修改指针指向的值有影响，因为指向的都是一个地方